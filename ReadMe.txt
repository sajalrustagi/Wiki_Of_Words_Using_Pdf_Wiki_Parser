Modules Needed To be installed using pip: PyPDF2, textract, wikipedia, enum, unicodedata, nltk, re

Further Steps needed:
nltk.download('punkt')
nltk.download('stopwords')

Finally Run Python file "WordsWikiHandler.py" which contains sample use of wiki of words and its creation along with addition of new file and fetching of top 100 words

